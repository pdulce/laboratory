package com.mylabs.pds.testrunner;

/*import org.junit.platform.launcher.JUnitLauncher;
import org.junit.platform.launcher.LauncherDiscoveryRequest;
import org.junit.platform.launcher.core.LauncherDiscoveryRequestBuilder;
import org.junit.platform.launcher.core.LauncherFactory;
*/

public class TestRunner {

    public static void main(String[] args) {
        // Configura la solicitud de descubrimiento
        /*LauncherDiscoveryRequest request = LauncherDiscoveryRequestBuilder
                .request()
                .selectors(selectClass(MyTest.class))
                .build();

        // Crea el lanzador
        JUnitLauncher launcher = LauncherFactory.create();

        // Ejecuta la prueba
        launcher.execute(request);*/
    }

}
